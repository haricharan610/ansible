---
 - name: nginx install and run
   hosts: frontend
   become: true
   tasks: 
 - name: install nginx server
   ansible.builtin.service:
    name: nginx
    state: started


 - name: start nginx
   ansible.builtin.package:
     name: nginx
     state: started.
