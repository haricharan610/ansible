---
 - name: nginx install and run
   hosts: frontend
   become: true
   tasks: 


 - name: install nginx server
   ansible.builtin.package:
   name: nginx
   state: present 


-  name: start nginx
   ansible.builtin.package:
   name: nginx
   state: started.
