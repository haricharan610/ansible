---
 -name: nginx install and run
hosts: local
become: true
tasks: 
 -name: install nginx server
ansible.builtin.debug
  name: nginx
state: present 


-name: start nginx
ansible.builtin.debug
 name: nginx
 state: started.
 