---
 - name: nginx install and run 
   hosts: frontend
   become: yes 
   tasks:
   - name: install nginx server 
     ansible.builtin.package:
      name: nginx
      state: present


   - name: nginx start
     ansible.builtin.service:
      state: started